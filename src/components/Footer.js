import React from "react";

const Footer = () => {
    return (
        <footer className="mt-8 text-center text-sm text-gray-500">
            © {new Date().getFullYear()} Ronald Zavaleta — Minimal design
        </footer>
    );
};

export default Footer;
